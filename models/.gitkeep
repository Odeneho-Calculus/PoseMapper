# This file ensures the models directory structure is preserved in Git
# The actual model files are excluded by .gitignore due to their large size
# Use download_models.py to download the required model files